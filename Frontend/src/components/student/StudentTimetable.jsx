import React from "react";

const StudentTimetable = ({ timetable, courses }) => (
  <div>
    <h2 className="text-2xl font-semibold mb-4">My Timetable</h2>
    <p className="text-gray-500">Timetable feature coming soon.</p>
    {/* You can render timetable data here if available */}
  </div>
);

export default StudentTimetable;
